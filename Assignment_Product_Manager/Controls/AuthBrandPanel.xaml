<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Assignment_Product_Manager.Controls.AuthBrandPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Assignment_Product_Manager.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="ThisControl">

    <Grid Background="{StaticResource BrandPrimaryBrush}">
        <StackPanel
            Padding="40"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="16" >
            <FontIcon FontSize="64"
                      Foreground="White"
                      Glyph="{x:Bind IconGlyph, Mode=OneWay}"/>
            <TextBlock FontSize="28" FontWeight="Bold" Foreground="White" TextAlignment="Center" Text="{x:Bind Headline, Mode=OneWay}"/>
            <TextBlock FontSize="14" Foreground="#BFDBFE" Text="{x:Bind Subtext, Mode=OneWay}" TextAlignment="Center" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</UserControl>
